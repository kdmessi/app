{% extends 'base.html.twig' %}

{% block title %}{{"New Genre"|trans}}{% endblock %}

{% block body %}
    <h1>{{"Create new Genre"|trans}}</h1>

    {{ include('genre/_form.html.twig') }}

    <a href="{{ path('genre_index') }}">{{"back to list"|trans}}</a>
{% endblock %}
