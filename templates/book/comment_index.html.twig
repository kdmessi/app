<div class="comment">
    <div class="nav text-center">
        {{ knp_pagination_render(comments) }}
    </div>
    {% for comment in comments %}
        <div class="card" style="width: 18rem;">
            <div class="card-body">
                <h5 class="card-title">Nick: {{ comment.nick }}</h5>
                <h6 class="card-subtitle mb-2 text-muted">Email:  {{ comment.email }}</h6>
                <p class="card-text">Comment: {{ comment.content }}</p>
                <a href="{{ path('comment_edit', {'id': comment.id}) }}" class="card-link">Edit</a>




{#{% include 'default/_form.html.twig' with {#}
{#                    page_title: 'title_category_delete',#}

{#                    attr: {#}
{#                        method: 'DELETE',#}
{#                        action: url('comment_delete', {'id': comment.id}),#}
{#                    },#}
{#                    form: form,#}
{#                    submit_label: 'action_delete'|trans,#}
{#                    back_to_list_path: 'autor_index',#}
{#                } %}#}




                {{ include('comment/_delete_form.html.twig') }}
            </div>
        </div>
    {% else %}
        <div>
            no records found
        </div>
    {% endfor %}
    <div class="nav text-center">
        {{ knp_pagination_render(comments) }}
    </div>
</div>